[project]
name = "study-helper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "AhmedSaeed4", email = "160440087+AhmedSaeed4@users.noreply.github.com" }
]
requires-python = ">=3.13"
dependencies = [
    "openai-agents>=0.6.1",
    "pypdf>=6.4.0",
    "python-dotenv>=1.2.1",
    "streamlit>=1.51.0",
    "utils>=1.0.2",
]

[project.scripts]
study-helper = "study_helper:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
